[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --ignore-glob=__init__.py
norecursedirs = .git .tox dist build *.egg
filterwarnings = 
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
